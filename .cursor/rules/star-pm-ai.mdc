---
description: Star PM AI 统一规则——写入/发版前必读 skill + CANONICAL
globs:
  - "**/*"
alwaysApply: true
---

# Star PM · AI 规则（指针）

## 写入或发版前（强制）

先 **Read** 本仓库 skill，再动手写 PM / 打 tag / `publish_release`：

`.cursor/skills/star-pm-write-release/SKILL.md`

字段总原则：**有则写之，无则跳过**（不编造）。

统一规则正文：`docs/ai/CANONICAL_RULES.md`

## 协作与验收

1. **要求不够明确 → 先细问再做**；问清后再实现  
2. **不写 spec / 长篇计划文件**；计划与需求**直接写进 PM**  
3. **测试要对齐初始要求**（+ PM 条目），再跑 tsc/单测/冒烟；未过不发版  
4. 改代码前 `compare_sources`；发版：CHANGELOG → version → commit → tag → `publish_release`  
5. 「同步版本」≠「同步 Git 更新」≠ `publish_release`  

MCP：先调 `get_ai_rules` 再大批量写入。  
跨机：拉最新仓库即可带上 `.cursor/skills/star-pm-write-release`。
