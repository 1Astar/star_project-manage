# 板块进程表 + 聊天工时 + 星球日历

**日期：** 2026-07-28  
**状态：** 已拍板，开做

## 结论

1. **迭代记录 · 板块演进**：默认表视图 — `板块 | 提出时间 | 改进记录 | 工时`；大板块 → 子板块缩进。  
2. **提出时间**：该路径下灵感 `occurredAt` 与演进 `createdAt` 取更早。  
3. **工时**：聊天时间戳写入开始/结束；条目层 + 板块行 Σ；不另立「AI 工时」产品名。  
4. **星球区**：星图 | 日历；日历 = 演进 + AI 变更会话，按日看项目/板块。

## 数据

| 实体 | 字段 |
|------|------|
| `studio_evolution_logs` | 新增 `work_started_at`、`work_finished_at`（可空） |
| `studio_change_sessions` | 复用 `created_at` / `finished_at` 作为时段 |
| 灵感 | 仅参与提出时间与 timeline 补充节点 |

## 非目标

- 预估工时（需求表）不动  
- 日历不含 git / 需求完成  
- 历史条目无时间戳则工时留空
