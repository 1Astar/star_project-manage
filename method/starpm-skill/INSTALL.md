# Install StarPM Method Skill (Cursor)

Copies this folder to `%USERPROFILE%\.cursor\skills\starpm-method\`.

```powershell
# From repo root, or pass -Source
.\method\starpm-skill\install.ps1
```

Then reload the Agent window / restart Cursor so the skill appears.

MCP is separate — see `CONNECT_MCP.md`.
